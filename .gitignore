# System files
.DS_Store
**/.DS_Store

# Stray screen captures accidentally saved into assets (not used by the site)
assets/Screenshot*.png

# Stray images dropped at the project root — the site only uses /assets
/*.jpg
/*.jpeg
/*.png
